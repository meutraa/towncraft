before_script:
  - apt-get update -qq && apt-get install -y -qq libsdl2-dev libsdl2-image-dev
  - ruby -v
  - which ruby
  - gem install bundler --no-ri --no-rdoc
  - bundle install --jobs $(nproc)  "${FLAGS[@]}"

main:
  script:
    - make